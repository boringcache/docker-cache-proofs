# Good Docker candidates for the graduated BuildKit-backend stack.
# These are the large-cache-graph / heavy-export-tail cases where the managed
# BuildKit backend is the high-scale lane (per docker lane positioning).
# Format: <case_id> <ref_key> <cache_lane> [build_output]
phentrieve-api release fresh
wormhole-solana main fresh
nmisp-nightly main fresh
kvrocks-docker main fresh
